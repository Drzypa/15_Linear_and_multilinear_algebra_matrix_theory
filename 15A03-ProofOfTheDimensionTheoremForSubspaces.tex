\documentclass[12pt]{article}
\usepackage{pmmeta}
\pmcanonicalname{ProofOfTheDimensionTheoremForSubspaces}
\pmcreated{2013-03-22 16:35:17}
\pmmodified{2013-03-22 16:35:17}
\pmowner{yark}{2760}
\pmmodifier{yark}{2760}
\pmtitle{proof of the dimension theorem for subspaces}
\pmrecord{5}{38782}
\pmprivacy{1}
\pmauthor{yark}{2760}
\pmtype{Proof}
\pmcomment{trigger rebuild}
\pmclassification{msc}{15A03}
%\pmkeywords{vector space}
%\pmkeywords{dimension}
%\pmkeywords{vector subspace}

\endmetadata

\usepackage{amsmath}

\def\dim{\operatorname{dim}}

\begin{document}
Let $S$ and $T$ be subspaces of a vector space.
By the rank-nullity theorem and the second isomorphism theorem (for modules)
we have
\begin{align*}
\dim(S+T) &= \dim S + \dim((S+T)/S) \\
          &= \dim S + \dim(T/(S\cap T)).
\end{align*}
Therefore
\begin{align*}
\dim(S+T) + \dim(S\cap T) &= \dim S + \dim(T/(S\cap T)) + \dim(S\cap T) \\
                          &= \dim S + \dim T,
\end{align*}
by the rank-nullity theorem again.


%%%%%
%%%%%
\end{document}
